// Snippet data coming from Firebase
export interface FirebaseSnippetData {
    code: string;
    compiler?: string;
    language: string;
    library?: string;
    revision: number;
    template: string;
}
