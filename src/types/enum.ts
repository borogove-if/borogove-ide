/**
 * FILE RESOURCES
 */
export enum MaterialsFileType {
    audio,
    code,
    folder,
    image,
    text,   // other text file
    data    // other binary file
}

/**
 * TABS
 */
export enum TabContentType {
    compiler,
    editor,
    interpreter,
    fileViewer,
    projectIndex,
    release,
    settings,
    welcome
}
